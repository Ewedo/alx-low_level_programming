#include <stdio.h>
/**
 * main - writes the character _putchar to stdout
 *
 * Return: On success 0.
 * On error, -1 is returned, and errno is set appropriately.
 */
int main( )
{
	printf("_putchar\n");
	return 0;
}

